/**
 * Algoritmo que imprime los 50 primeros nuemros pares for
 */

 //for(let i = 0; i < 100; i = i + 1){
   // console.log(`Valor de i = ${i}`);
 //}

 /**
  * 
  * Algoritmo que imprime los múltiplos de 5 entre los numeros 0 y 100
  */

  // i++  = i = i + 1

  //for (let i = 0; i < 100; i++){
        //if ( i % 5 === 0){
        //console.log(`Múltiplo de 5 = ${i}`);
        //}
  //}

  /**
   * 
   * Imprimir la sumatoria de los 20 primeros números a partir de 0
   */


  //let suma = 0;
   //for (let i = 0; i < 20; i + i++){
       //suma = suma + i;
   //}
       //console.log(i);
   

       /**
        * 
        * Algoritmo qwue imprime el factorial de un número 
        * n (ejemplo del nuemro 6)
        * 
        */

        //let factorial = 1;
        //let numero = 8;
        //for (let i = 1; i <= numero; i++){
        //    factorial = factorial * i;
        //}

        //console.log(`Factorial de %{numero} = ${factorial}`);

        /**
         * 
         * Algoritmo para calcular la multiplicación de 2 numeros sin usar el operador (*)
         */

        // let a = 9;
         //let b = 4;
         //let suma = 0;

         //for( let i = 0; i < a; i++){
         //   suma += b;
         //}

         //// suma += b; => suma = suma + b

         //console.log(suma);




        // Ciclo While
        
        
        //  Mientras que i sea menor a 30, colocaremos los multiplos de 10

        //let i = 0;

        //while (i < 30){
          //if (i % 10 === 0){
            //console.log(`Multiplo de 10 = ${i}`);

          //}
          //i++;

        //}
    
      /**
       * 
       * Usamos While por lo general cuando no sabemos cuantas vueltas tenemos queiterar o repetir el código
       */

       //Algoritmo para recibir n notas (no sé cuantas notas)
      // La única forma de registrar el fin del ingreso de notas será colocando el valor de -1
      // Ejm: 15 20 05 09 -1
      // En ejemplo, se calculrá el promedio de las primeras 5 notas.

      //let suma = 0;
      //let nota = 0;
      //let contador = 0;
      //while (nota !== -1) {
      //  nota = +prompt("Ingresa una nueva nota o //ingresa -1 para terminar")
      //  if (nota !== -1){
      //  suma = suma + nota;
      //  contador++;
      //  }

      //}
      //console.log(`Nro. de notas ingresadas : $//{contador}`);
      //console.log(`Promedio de notas = ${suma / //contador}`);
      


      